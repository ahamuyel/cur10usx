{
  "installCommand": "yum install -y libatomic 2>/dev/null || apt-get install -y libatomic1 2>/dev/null || true && npm install",
  "buildCommand": "npm run vercel-build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/cron/snapshot",
      "schedule": "0 6 * * *"
    }
  ]
}
